#ifndef EXAMPLE_H
#define EXAMPLE_H
// 可以写一些内容，表示当前这文件中的内容只会被编译一次，无论是被多少个源文件所包含。同样也可以使用#pragma once进行设置
#endif

#pragma once


namespace geometry {
namespace area {
double circle(double area);
}
} // namespace geometry

#pragma once
